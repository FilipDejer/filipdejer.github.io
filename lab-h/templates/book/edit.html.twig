{% extends 'base.html.twig' %}

{% block title %}Edycja książki{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mb-4">Edytuj książkę</h1>

        <div class="card shadow-sm mb-4">
            <div class="card-body">
                {{ include('book/_form.html.twig', {'button_label': 'Zapisz zmiany'}) }}
            </div>
        </div>

        <div class="d-flex gap-2">
            <a href="{{ path('app_book_index') }}" class="btn btn-secondary">Powrót do listy</a>
            {{ include('book/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
